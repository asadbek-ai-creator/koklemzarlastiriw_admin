# API base URL (leave empty to use Vite dev proxy)
# When set, axios calls go directly to this origin (e.g. for production builds)
VITE_API_URL=

# Enable MSW mock service worker for local development
# Set to "true" to intercept all API calls with mock data
VITE_USE_MOCK=false
